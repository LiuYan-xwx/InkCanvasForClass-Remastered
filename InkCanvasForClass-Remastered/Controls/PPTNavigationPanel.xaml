<UserControl
    x:Class="InkCanvasForClass_Remastered.Controls.PPTNavigationPanel"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:InkCanvasForClass_Remastered.Controls"
    xmlns:converters="clr-namespace:InkCanvasForClass_Remastered.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewmodels="clr-namespace:InkCanvasForClass_Remastered.ViewModels"
    d:DesignHeight="800"
    d:DesignWidth="1200"
    mc:Ignorable="d">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
    </UserControl.Resources>

    <Grid>
        <!--  Left Side Panel  -->
        <Viewbox
            Width="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=controls:PPTNavigationPanel},Path=PanelWidth}"
            HorizontalAlignment="Left"
            VerticalAlignment="Center"
            Visibility="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=controls:PPTNavigationPanel},Path=LeftButtonVisibility}">
            <controls:PPTNavigationButton
                DataContext="{Binding}"
                NextClick="PPTButton_NextClick"
                PageClick="PPTButton_PageClick"
                PreviousClick="PPTButton_PreviousClick" />
            <Viewbox.RenderTransform>
                <TranslateTransform Y="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=controls:PPTNavigationPanel},Path=LeftButtonOffset}"/>
            </Viewbox.RenderTransform>
        </Viewbox>

        <!--  Right Side Panel  -->
        <Viewbox
            Width="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=controls:PPTNavigationPanel},Path=PanelWidth}"
            HorizontalAlignment="Right"
            VerticalAlignment="Center"
            Visibility="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=controls:PPTNavigationPanel},Path=RightButtonVisibility}">
            <controls:PPTNavigationButton
                DataContext="{Binding}"
                NextClick="PPTButton_NextClick"
                PageClick="PPTButton_PageClick"
                PreviousClick="PPTButton_PreviousClick" />
            <Viewbox.RenderTransform>
                <TranslateTransform Y="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=controls:PPTNavigationPanel},Path=RightButtonOffset}"/>
            </Viewbox.RenderTransform>
        </Viewbox>
    </Grid>
</UserControl>