# MainWindow 重构路线

按当前分支和会话中已确认的方向整理，更新于 2026-09-08。

## 目标和范围

先让 MainWindow 的交互和界面状态容易读、容易改，再逐步使用现有 MainViewModel 和 XAML 绑定。

- 主要处理 MainWindow 和相关界面，后续再逐个整理其他窗口。
- 普通点击使用 Button；浮动栏优先复用 FloatingBarButton，颜色选择使用 ColorSwatchButton。
- 浮动栏及其面板中的按钮不接收键盘焦点、不参与 Tab 导航，沿用当前视觉样式。
- 小范围收敛重复代码，不再把 MainWindow 拆成多个 partial 文件，也不引入 Controller 等新架构。
- 启动流程、SettingsService、PowerPointService 和可读、可用的现有业务逻辑暂时保持现状。

## 阶段进度

| 阶段 | 状态 | 交付目标 |
| --- | --- | --- |
| 1. 规范点击控件 | 已完成主要改造 | 真实按钮、复用浮动栏按钮、颜色按钮封装、清理旧鼠标事件、取消键盘焦点 |
| 2. 收敛面板与工具界面代码 | 已完成 | 统一面板开关，分开关闭面板、工具样式、浮动栏居中的实现，替换工具模式字符串 |
| 3. 统一当前工具状态 | 已完成 | 工具按钮和白板按钮通过现有 ViewModel 属性同步选中样式 |
| 4. 整理画笔与橡皮选项 | 已完成 | 普通笔/荧光笔、圆形/矩形橡皮、颜色选中状态由绑定驱动 |
| 5. 整理白板页面界面 | 下一步 | 集中页面列表数据和按钮状态，减少左右两套界面的重复代码 |
| 6. 逐个整理其他窗口 | 后续 | 沿用已稳定的控件和写法，按实际重复程度决定提取范围 |

## 阶段 2：面板逻辑（已完成）

- `ToggleToolbarPanels` 统一工具菜单、手势、调色盘、浮动栏橡皮面板的开关；每组同步浮动栏和白板上的面板。
- `HideAllSubPanels` 集中维护关闭逻辑，支持动画关闭和立即关闭。页面列表继续独立开关，全局收起时一起关闭。
- `HideSubPanels` 保留调用入口，工具样式交给 `UpdateToolbarToolAppearance`，延迟居中交给 `CenterFloatingBarAsync`。
- 工具参数改为现有的 `InkCanvasEditingMode`，合并画笔与颜色操作的相同模式，清除过时注释。
- 倒计时、随机点名和墨迹文件操作复用现有 `HideToolsPanel`。

同时补齐两处面板关闭遗漏：打开浮动栏橡皮面板时关闭手势面板；立即收起时也关闭两侧手势面板。白板橡皮按钮原有的单独开关行为继续保留。

## 阶段 3：当前工具状态（已完成）

- 现有 `MainViewModel.AppPenMode` 作为用户选定工具的状态来源；补齐白板橡皮、颜色切换、白板模式切换等入口。
- 浮动栏和白板的选中图标、前景、背景及指示条位置由 XAML 绑定驱动，删除 `UpdateToolbarToolAppearance` 及各按钮里的手动高亮赋值。
- 移除 `HideSubPanels` 的工具参数，关闭面板不再更新工具选中样式。
- 用明确的工具状态替换 `Pen_Icon.Background == null` 判断；提取 `ActivatePen`，内部启用画笔不再借用会开关调色盘的点击事件。
- 白板墨迹擦复用现有墨迹擦入口；删除没有调用或 XAML 绑定的旧画布开关方法。
- 触摸擦除和手势的临时模式不改变用户选定的工具；手势结束、多指书写开关恢复选定工具。鼠标模式在清除选择后明确关闭画布编辑。
- 提交选中墨迹的绘图属性历史时保留当前工具样式；批注/白板清屏回到画笔，屏幕鼠标模式清屏保持穿透。

验收已覆盖：首次点击切换工具，再次点击开关选项；两套工具栏显示一致；临时编辑模式不改变选中样式；手势结束恢复选定工具；鼠标模式、清屏、白板进出、放映翻页和按钮不抢焦点。

## 阶段 4：画笔与橡皮选项（已完成）

1. 删除 `CheckPenTypeUIState`、`CheckEraserTypeTab` 以及两套界面的重复样式赋值。
2. 使用 `DrawingAttributes.IsHighlighter`、现有 `Settings` 和 `MainViewModel` 属性作为状态来源，通过绑定控制选项卡样式、内容显隐和数值同步。
3. 保留并复用 `FloatingBarButton`、`ColorSwatchButton`，两套调色盘的选中状态统一绑定到 ViewModel。
4. 画笔类型切换同时更新实际笔尖形状和尺寸；橡皮形状只修改现有设置，由 `UpdateEraserShape` 统一更新画布。
5. 画笔颜色、透明度、笔锋样式、粗细和主题切换保留原有业务行为，并覆盖桌面/白板的颜色记忆。
6. 透明度通过 `MainViewModel.InkAlpha` 读写：修改 Alpha 后写回完整 `Color`，监听绘图属性的颜色变化并通知界面。两处滑块双向绑定此属性，删除旧 `ValueChanged` 处理，避免直接绑定值类型的 `Color.A` 无法回写实际颜色。

验收已覆盖：两种笔、两种橡皮、全部颜色、选中样式、面板动画、画笔参数双向同步、透明度和 RGB 保留、颜色主题、实际笔尖/橡皮尺寸、桌面/白板颜色记忆，以及原有撤销/重做和白板回归。

## 随后：白板页面界面

1. 整理页面列表数据、当前页和上一页/下一页按钮状态。
2. 让左右页面列表共享同一份页面数据，集中刷新入口。
3. 保留现有页面存取和历史处理，先移除界面层的重复更新。
4. 校验翻页、新增/删除页面、缩略图点击、选中墨迹复制到新页和页面数量边界。

## 验证记录

- 本轮 Debug 构建通过，0 个错误。
- 临时 WPF 验证程序共通过 2705 项断言：画笔/橡皮选项绑定、实际笔尖和橡皮尺寸、参数双向同步、透明度和颜色记忆检查；阶段 1–3 的工具、面板、选择、页面和键盘回归检查全部通过。
- 透明度专项补充 113 项断言，已复现并修复直接绑定 `Color.A` 时滑块变化未写回实际画笔颜色的问题；覆盖两处滑块及数字同步、RGB 保留、透明度边界、代码改色和替换绘图属性对象后的通知。原先依靠事件写回的检查不足以验证双向绑定，现已删除该事件后重新验证。
- 验证使用实际 WPF 控件，未展示应用窗口；实际触屏、手写笔和真实 PowerPoint 放映交互仍需人工体验确认。

后续每批完成时更新本文件的进度与验证记录。
